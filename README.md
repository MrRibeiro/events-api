# **Events API**

API CRUD de eventos utilizando Symfony

Subir o ambiente:

```sh
docker-compose up -d
```

Instalar as dependencias:

```sh
composer install
```

Execute o servidor:

```sh
symfony serve
```
